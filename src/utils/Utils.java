package utils;
/*
 * class to hold helpful utility functions
 */

public class Utils {
    // takes in a char and returns a string only containing that char
    public static String charToString(char c) {
        return String.format("%c", c);
    }
}
